---
"@polymarket/react": minor
---

Initial `@polymarket/react` release: `PolymarketProvider` with `createConfig`, the `useClientAction` and `usePaginatedAction` read primitives with `skip` pausing, and the discovery hooks `useMarket`, `useMarkets`, `useEvents`, and `useOrderBook`.
